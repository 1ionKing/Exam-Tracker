:root {
  --bg-color: #f4f4f5;
  --card-bg: #ffffff;
  --text-main: #1e293b;
  --text-secondary: #94a3b8;
  --accent-teal: #20c997;
  --accent-blue: #0369a1;
  --accent-orange: #f59e0b; /* For the countdown */
  --border-color: #e2e8f0;
  --font-system: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body.app-body {
  font-family: var(--font-system);
  background-color: var(--bg-color);
  color: var(--text-main);
  padding-bottom: 90px; 
  -webkit-font-smoothing: antialiased;
}

.app-header {
  display: flex; align-items: center; justify-content: space-between;
  padding: 16px 20px; background-color: var(--bg-color);
}
.app-header__menu-btn { background: none; border: none; color: var(--text-main); cursor: pointer; width: 24px; }
.app-header__title-group { display: flex; flex-direction: column; align-items: center; }
.app-header__title { font-size: 1.125rem; font-weight: 600; color: var(--text-main); }
.app-header__subtitle { font-size: 0.65rem; font-weight: 700; color: var(--accent-blue); letter-spacing: 0.05em; margin-top: 2px; }
.app-header__spacer { width: 24px; }

.app-main { padding: 0 20px 20px 20px; display: flex; flex-direction: column; gap: 20px; }
.card { background-color: var(--card-bg); border: 1px solid var(--border-color); border-radius: 16px; padding: 20px; }

/* --- Countdown Card --- */
.countdown-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background-color: var(--text-main); /* Dark background matching the screenshot header */
  color: #ffffff;
  border: none;
}
.countdown-card__config { display: flex; flex-direction: column; gap: 8px; }
.countdown-card__label { font-size: 0.85rem; font-weight: 500; color: #cbd5e1; }
.countdown-card__input {
  background: var(--card-bg);
  border: none;
  padding: 8px 12px;
  border-radius: 8px;
  font-family: var(--font-system);
  font-weight: 600;
  color: var(--text-main);
  outline: none;
}
.countdown-card__display {
  display: flex;
  align-items: center;
  gap: 8px;
  text-align: left;
}
.countdown-card__number {
  font-size: 2.5rem;
  font-weight: 700;
  color: var(--accent-orange);
}
.countdown-card__text {
  font-size: 0.75rem;
  font-weight: 700;
  line-height: 1.2;
  letter-spacing: 0.05em;
  color: #cbd5e1;
}

/* --- Progress Summary --- */
.progress-card { display: flex; align-items: center; gap: 20px; }
.progress-card__indicator {
  width: 64px; height: 64px; border-radius: 50%;
  background: conic-gradient(var(--border-color) 100%, transparent 0);
  display: flex; align-items: center; justify-content: center; position: relative; flex-shrink: 0;
}
.progress-card__indicator::before {
  content: ""; position: absolute; width: 50px; height: 50px; background-color: var(--card-bg); border-radius: 50%;
}
.progress-card__percentage { position: relative; font-size: 0.85rem; font-weight: 700; color: var(--text-main); }
.progress-card__content { display: flex; flex-direction: column; gap: 4px; }
.progress-card__title { font-size: 1.1rem; font-weight: 600; }
.progress-card__desc { font-size: 0.85rem; color: var(--text-secondary); }

/* --- Syllabus Tracker --- */
.syllabus-tracker__title { font-size: 1.1rem; font-weight: 600; margin-bottom: 12px; padding-left: 4px; }
.topic-list { list-style: none; display: flex; flex-direction: column; gap: 16px; }
.topic-item { display: flex; align-items: center; justify-content: space-between; padding-bottom: 16px; border-bottom: 1px solid var(--border-color); }
.topic-item:last-child { padding-bottom: 0; border-bottom: none; }
.topic-item__left { display: flex; align-items: center; gap: 16px; }

.topic-item__checkbox {
  appearance: none; -webkit-appearance: none; width: 24px; height: 24px;
  border: 2px solid var(--border-color); border-radius: 6px; outline: none; cursor: pointer; position: relative; flex-shrink: 0;
}
.topic-item__checkbox:checked { background-color: var(--accent-teal); border-color: var(--accent-teal); }
.topic-item__checkbox:checked::after {
  content: ''; position: absolute; left: 7px; top: 3px; width: 5px; height: 11px;
  border: solid white; border-width: 0 2px 2px 0; transform: rotate(45deg);
}

.topic-item__info { display: flex; flex-direction: column; gap: 2px; }
.topic-item__subject { font-size: 0.70rem; font-weight: 500; color: var(--text-secondary); }
.topic-item__name { font-size: 0.95rem; font-weight: 600; color: var(--text-main); }
.topic-item__name.is-completed { color: var(--text-secondary); text-decoration: line-through; }
.topic-item__tag { font-size: 0.7rem; font-weight: 600; padding: 4px 10px; border-radius: 12px; text-transform: uppercase; }
.topic-item__tag--pending { background-color: #f1f5f9; color: var(--text-secondary); }
.topic-item__tag--completed { background-color: #ecfdf5; color: var(--accent-teal); }

/* --- Bottom Bar --- */
.bottom-bar {
  position: fixed; bottom: 0; left: 0; width: 100%; background-color: var(--card-bg);
  border-top: 1px solid var(--border-color); padding: 16px 20px; display: flex; align-items: center; justify-content: space-between;
}
.bottom-bar__summary { display: flex; flex-direction: column; gap: 2px; max-width: 60%; }
.bottom-bar__label { font-size: 0.75rem; color: var(--text-secondary); font-weight: 500; }
.bottom-bar__topic { font-size: 0.9rem; font-weight: 600; color: var(--text-main); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.bottom-bar__action-btn {
  background-color: var(--accent-blue); color: #ffffff; border: none; border-radius: 8px;
  padding: 12px 20px; font-size: 0.9rem; font-weight: 600; cursor: pointer;
}
/* Add this right above the /* --- Bottom Bar --- */ section */

/* --- Search Bar --- */
.search-container {
  margin-bottom: 20px;
}

.search-input {
  width: 100%;
  padding: 14px 16px;
  border: 1px solid var(--border-color);
  border-radius: 8px;
  font-family: var(--font-system);
  font-size: 0.9rem;
  color: var(--text-main);
  background-color: #f8fafc;
  outline: none;
  transition: all 0.2s ease;
}

.search-input::placeholder {
  color: var(--text-secondary);
}

.search-input:focus {
  border-color: var(--accent-blue);
  background-color: #ffffff;
  box-shadow: 0 0 0 3px rgba(3, 105, 161, 0.1);
}